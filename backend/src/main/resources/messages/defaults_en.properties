# Default Names and Suffixes (English)

# Default name for the main business branch if not provided during creation
default.branch.main.name=Main Store

# Suffix added to the default brand name created on business activation
default.brand.suffix.own=(Own Brand)

# Suffix added to the default supplier name created on business activation
default.supplier.suffix.self=(Self-Supply)